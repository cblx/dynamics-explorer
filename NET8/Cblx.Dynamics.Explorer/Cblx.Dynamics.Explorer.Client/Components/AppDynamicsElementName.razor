@(LogicalName ?? DisplayName ?? CustomName)
@code {
    [Parameter]
    public required string? LogicalName { get; set; }
    [Parameter]
    public required string? DisplayName { get; set; }
    [Parameter]
    public required string? CustomName { get; set; }
}
